{% extends 'home/template.html.twig' %}

{% block title %}Hello profileController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>
			{{ controlle_name }}</h1>
		{% for user in users %}


			<div class="col-3">
				<div class="card" style="width: 18rem;">
					<div class="card-body">
						{{  user.Picture }}
						<h6 class="card-subtitle mb-2 text-muted">
							{{  user.username }}</h6>
						{% for role in user.roles%}
							{% if role == "ROLE_USER" or role == "ROLE_ADMIN" %}
								<a href="{{ path('app_DelProfile', {id: user.id}) }}" class="card-link">Suprimer</a>
								<a href="{{'/profile/'}}" class="card-link">Détail</a>
							{% endif %}
						{% endfor %}
						<h6 class="card-subtitle mb-2 text-muted">
							{{  user.LastName }}</h6>
					</div>
				</div>
			</div>
		{% endfor %}
	</div>
{% endblock %}
