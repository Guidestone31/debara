{% extends 'home/template.html.twig' %}

{% block title %}My Cart!
{% endblock %}

{% block body %}

	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	<div class="card text-center">
		<div class="card-header">
			Modification Annonce
		</div>
		<div class="card-body">
			<form action="{{path('app_paiement', {id: annoucements.id}) }}" method="post">
				<h5 class="card-title">Offre exeptionnelle !</h5>
				<p class="card-text">Cette offre vous permet de modifier votre annonce !!</p>
				<p class="card-text">2 €</p>
				<input type="hidden" name="token" value="{{csrf_token('myform')}}">
				<input type="hidden" name="amount" value="1">
				<button class="btn">
					<span class="btn btn-primary">Pay with paypal</span>
				</button>
			</form>

		</div>
	</div>
{% endblock %}
